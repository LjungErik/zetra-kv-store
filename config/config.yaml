# Cluster peers — shared across all nodes.
# Per-node settings (id, addresses, data_dir) are set via environment variables.
cluster:
  peers:
    - id: "node1"
      address: "node1:7001"
    - id: "node2"
      address: "node2:7002"
    - id: "node3"
      address: "node3:7003"
